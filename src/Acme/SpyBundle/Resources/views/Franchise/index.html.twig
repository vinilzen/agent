{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Franchise list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Brand</th>
                <th>Industry</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('franchise_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.brand }}</td>
                <td>{{ entity.industry }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('franchise_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('franchise_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('franchise_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
